seed: 42
paths:
  raw_data: data/raw
  artifacts: artifacts
task: hotel_cancellation
train:
  target: is_canceled
  test_size: 0.25
  date_col: null
  model: "xgboost"
  model_args:
    learning_rate: 0.05
    max_depth: 3
    n_estimators: 100