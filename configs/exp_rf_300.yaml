seed: 42
paths:
  raw_data: data/raw
  artifacts: artifacts
task: hotel_cancellation
train:
  target: is_canceled
  test_size: 0.25
  date_col: null
  model: "random_forest"
  model_args:
    n_estimators: 300