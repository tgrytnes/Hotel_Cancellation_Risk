seed: 42
paths:
  raw_data: data/raw
  artifacts: artifacts
task: hotel_cancellation
train:
  target: is_canceled
  test_size: 0.25
  date_col: null  # No date column for chronological split in synthetic data
  # Model choice: logreg_baseline (recommended) | logreg_l1 | logreg_sklearn
  model: "logreg_baseline"
  # Model-specific args (can override defaults)
  model_args: {}